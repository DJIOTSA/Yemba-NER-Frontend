# staticSites:
#   - name: Yemba-NER-Frontend
#     buildCommand: "" # No build command is needed for plain HTML/CSS/JS
#     publishPath: pages # Directory to publish

services:
  - type: web
    name: yembanerui
    env: static
    buildCommand: ""
    staticPublishPath: ./
